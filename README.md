# Affordable GPU CI with Modal

Dummy Python project setup for experimenting with GPU-accelerated CI using [Modal](https://modal.com).

The sample package (`sample_pkg`) contains only a trivial `power` function and a set of tests — the focus is on the CI/CD infrastructure, not the code itself.
